### 01.、说说常见的Linux系统.

```sh
Debian系统，ubt系统，kali系统，centos系统，rocky，麒麟，欧拉
```



### 02. **Linux系统最高权限用户是谁?他的名字是大写还是小写?**

```sh
root 小写
```



##	03. 如何查询红帽系统ip地址?

```sh 
ifconfig
```

##	04. 说两个你熟悉的Linux命令及他们的作用?

```sh
vim 它可以编辑文件内容或创建文件
cd 它是用来进入目录的一个命令
```



##	05. 什么是Shell?

Shell是用户与操作系统内核之间的交互接口，也是一种脚本编程语言。它的核心作用是接收用户输入的命令，解析并调用系统内核或其他程序执行任务，同时将结果反馈给用户。

##	06. [root@oldboy-c7 /etc/sysconfig/ ]#  说说每一部分含义?

root 表示用户（用户名）

oldboy-c7 表示主机名 

/etc/sysconfig/当前所在的位置的路径

# 表示用户类型为root

##	07. 说一个错误提示及含义?

login incorrect 它所表示的含义是登录失败，密码错误

##	08. 说说Linux命令格式,什么是选项和参数?

```sh
命令 选项 参数 
选项是同一个命令的不同功能
参数是命令处理的目标
```



##	09. 根据要求执行相应的命令(进行操作并做记录,操作,操作,操作)

### 01) 显示/etc/目录下面内容的详细信息

```sh
ll /etc/
```

### 02) 进入到 /etc/sysconfig/network-scripts/目录然后显示当前所在目录.

```sh
[root@kylin-10.0.0.200 ~]# cd /etc/sysconfig/network-scripts/
[root@kylin-10.0.0.200 /etc/sysconfig/network-scripts]# pwd
/etc/sysconfig/network-scripts
```



### 03) 返回当前用户家目录，复制/etc/hosts 到当前用户家目录下面.

```sh
[root@kylin-10.0.0.200 /etc/sysconfig/network-scripts]# cd
[root@kylin-10.0.0.200 ~]# cp /etc/host
host.conf  hostname   hosts      
[root@kylin-10.0.0.200 ~]# cp /etc/hosts .
```

### 04) 显示/etc/passwd 文件的内容

```sh
cat /etc/passwd
```

### 05) **在根下创建目录oldboy-83,然后在oldboy-83下面创建 lidao.txt,然后追加 oldboy83 linux 到lidao.txt中,然后显示lidao.txt文件内容.**

```sh
[root@kylin-10.0.0.200 ~]# mkdir /oldboy-83
[root@kylin-10.0.0.200 ~]# touch /oldboy-83/lidao.txt
[root@kylin-10.0.0.200 ~]# echo oldboy83 linux > /oldboy-83/lidao.txt
[root@kylin-10.0.0.200 ~]# cat /oldboy-83/lidao.txt
oldboy83 linux
```

### 06) **复制 lidao.txt 到/oldboy-83/并改名oldboy.txt**

```sh
cp /oldboy-83/lidao.txt /oldboy-83/oldboy.txt
```

### 07) 一键创建多层目录/oldboy/lidao/old01/old02/old03/old0011

```sh
mkdir -p /oldboy/lidao/old01/old02/old03/old0011
```



### 11) 假设在 /etc/sysconfig/network-scripts/下面 然后你进入了 /tmp/目录,请问如何快速回到/etc/sysconfig/network-scripts目录下面.

```sh
cd /etc/sysconfig/network-scripts
```



## 10) Linux无法远程连接排查流程及使用的命令.

第一步：检查使用ping命令去 ping baidu.com 检查虚拟机是否可以连接百度 

第二步：检查vm虚拟机的网络编辑器是否有问题 

第三步：检查虚拟机是否配置上ip地址 

第四步：检查虚拟机设置是否正确

命令有：

```
ping baidu.com
nmtui
```



## 11) 备份目录/oldboy-83 到 /backup下 并改名叫 oldboy-83-backup 

```sh
cp -r /oldboy-83 /backup/oldboy-83-backup
```



## 12）检查www.baidu.com的80端口是否开启

```sh
telnet www.baidu.com 80
```



## 13）检查www.jd.com的22端口是否开启

```sh
telnet www.jd.com 22
```



## 14）检查到223.5.5.5ip的连通性

```sh
ping 223.5.5.5
```



###  显示/root目录下所有文件(含隐藏)

```sh
ll -a /root
```

-  



### 08) 复制网卡配置文件到/oldboy-83/目录下.

```sh
cat /etc/sysconfig/network-scripts/ >> /oldboy-83/
```

### 9) **修改/oldboy-83目录名字为/oldboy-83-lnb**

```sh
mv /oldboy-83 /oldboy-83-lnb
```



### 10) 创建/backup/20210308 目录,然后把/oldboy-83或者/oldboy-83-lnb 复制到  /backup/20210308/下.

```sh
mkdir /backup/20210308
cp /oldboy-83 /backup/20210308/
```



##	 如何检查Linux 是否能上网?

```sh
ping baidu.com
```



# 下面是扩展题可以尝试下



# 实战操作题:

## 1. 实战练习题：网站部署

#### **背景**

你刚刚加入"老男孩教育99期"担任初级云计算运维工程师。部门主管给你布置了第一个任务：

> "公司新项目需要快速部署一套测试用的Nginx Web服务环境，具体要求如下：
>
> 1. 在`/opt/project`目录下建立规范的网站目录结构
> 2. 创建测试页面并配置虚拟主机
> 3. 保证服务开机自启
> 4. 最后清理临时文件
>    这是检验你基础运维能力的重要任务！"



#### **任务分解**

**1. 构建标准目录结构**

 

```sh
# 创建项目根目录及子目录（使用mkdir）
/opt/project
├── html       # 网站主目录
├── config     # 配置文件目录
├── logs       # 日志目录
└── backup     # 备份目录
根据上面目录结构进程创建,创建对应的目录

# 生成测试页面（使用{}生成序列文件）
在html目录下创建：
- index.html
- product{1..3}.html

在backup目录下创建oldboy-backup{01..10}.bak 10个文件
```

**2. 配置虚拟主机**

```
在config目录下创建oldboy_project.conf文件并写入如下内容:
server {
    listen 80;
    server_name project.test;
    root /opt/project/html;
    access_log /opt/project/logs/access.log;
}
```

**3. 服务管理**

```
这步骤先略过.
```

**4. 清理工作**

```
删除/backup/目录下所有内容.
 
```







## 2. 实战练习题：服务器初始化配置与日志收集

------

#### **背景**

你作为"星云科技"新入职的运维工程师，接手了部门新采购的10台服务器。技术主管给出紧急任务：

> "所有服务器需在今日完成基础环境初始化，具体要求：
>
> 1. 建立运维专用目录结构
> 2. 批量创建测试文件
> 3. 备份关键系统配置
> 4. 配置服务监控检查
> 5. 生成初始化报告
>    这是生产环境服务器，操作必须规范！

#### **任务分解**

**1. 创建运维目录体系**

 :warning:注意项目所有操作都相对于 /opt/cloud_ops目录.

```sh
# 使用mkdir创建多级目录（注意使用-p参数）
/opt/cloud_ops
├── backups      # 备份目录
├── scripts      # 脚本目录
├── reports      # 报告目录
└── temp         # 临时目录
```

**2. 生成测试数据集**

 

```sh
# 使用{}批量创建日志模拟文件（注意文件命名规范）
在/opt/cloud_ops/temp下创建：
- web_access_{1..5}.log
- db_query_{a..c}.log

# 使用echo生成系统信息文件
echo "Kernel: $(uname -r)" 运行左边的命令把结果写入到文件:/opt/cloud_ops/reports/system_info.txt中(思考下重定向符号)
```

**3. 系统配置备份**

 

```
# 使用cp备份关键配置文件（注意保留原属性）
把/etc/hosts  /etc/hostname  /etc/passwd  /etc/resolv.conf复制到/ /opt/cloud_ops/backups/
 

# 使用>>追加备份记录
echo "$(date) 完成系统配置备份" 追加到/opt/cloud_ops/reports/operation.log
```

**4. 服务状态检查**

 

```
# 使用systemctl检查关键服务（结果输出到报告）
systemctl status sshd  写入到文件中 /opt/cloud_ops/reports/service_status.txt
systemctl is-active firewalld 追加到文件中. /opt/cloud_ops/reports/service_status.txt

# 使用cat合并报告
把/opt/cloud_ops/reports/system_info.txt和/opt/cloud_ops/reports/service_status.txt 2个文件内容合并到 final_report.txt文件中.
```

**5. 环境清理**

 

```
# 使用rm安全清理临时文件（先确认文件列表）
删除temp下所有内容.
```